// Add a click event listener to each video thumbnail
const videoThumbnails = document.querySelectorAll('.video img');

videoThumbnails.forEach(thumbnail => {
  thumbnail.addEventListener('click', () => {
    // Code to open the video modal goes here
  });
});

